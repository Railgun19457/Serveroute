prefix = "<gray>[<gradient:#7dd3fc:#38bdf8>Serveroute</gradient>]</gray> "

no-permission = "<red>你没有权限。</red>"
players-only = "<red>该命令只能由玩家执行。</red>"

server.list-header = "<white>可用服务器：</white>"
server.list-entry = "<gray>- <white>{display}</white> <dark_gray>({id})</dark_gray></gray>"
server.list-entry-current = "<gray>- <green>{display}</green> <dark_gray>({id})</dark_gray> <aqua>[当前]</aqua></gray>"
server.list-empty = "<yellow>没有可加入的服务器。</yellow>"
server.not-found = "<red>找不到服务器 <white>{input}</white>。</red>"
server.already-connected = "<yellow>你已经在 <white>{display}</white>。</yellow>"
server.connecting = "<gray>正在前往 <white>{display}</white>…</gray>"
server.internal-missing = "<red>后端 <white>{target}</white> 未在代理中注册。</red>"
server.transfer-old-client = "<red>前往 <white>{display}</white> 需要 Minecraft 1.20.5 或更高版本。</red>"
server.connect-fail = "<red>无法连接到 <white>{display}</white>：{reason}</red>"

line.list-header = "<white>可用线路：</white>"
line.list-entry = "<gray>- <white>{display}</white> <dark_gray>({id})</dark_gray></gray>"
line.list-entry-current = "<gray>- <green>{display}</green> <dark_gray>({id})</dark_gray> <aqua>[当前]</aqua></gray>"
line.list-empty = "<yellow>没有可切换的线路。</yellow>"
line.unknown-current = "<gray>无法识别当前线路（请使用域名连接）。</gray>"
line.not-found = "<red>找不到线路 <white>{input}</white>。</red>"
line.already-on = "<yellow>你已经在 <white>{display}</white> 线路。</yellow>"
line.switching = "<gray>正在切换到 <white>{display}</white> 线路…</gray>"
line.old-client = "<red>切换线路需要 Minecraft 1.20.5 或更高版本。</red>"

admin.reloaded = "<green>配置已重载。</green>"
admin.reload-failed = "<red>重载失败，已保留当前配置：{error}</red>"
